# Motor de reposición

La duración se calcula por SKU: `servingsTotal / servingsPerDay`. `estimatedDepletionDate` y `reorderContactDate` se guardan en `product_usage`; no se usa una regla universal de día 25. La automatización debe exigir consentimiento de contacto y confirmar disponibilidad al iniciar una recompra.
