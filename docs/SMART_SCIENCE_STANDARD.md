# Smart Science Standard

Los contenidos se clasifican como `SOLID`, `EMERGING` o `MECHANISTIC` mediante `EvidenceBadge` y `EvidenceNotice`. Un mecanismo no es una promesa comercial. Todo artículo futuro debe incluir fuentes, autor, fecha de revisión, fecha de actualización y el disclaimer educativo.
